name: Build and Verify

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  build-and-verify:
    runs-on: ubuntu-20.04
    if: contains(github.event.issue.labels.*.name, 'ok-to-test')
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Verify PR
        id: verify_pr
        continue-on-error: true
        run: |
          echo "Hi"
